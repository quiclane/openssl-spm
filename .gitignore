# Local OpenSSL source checkout (build input; CI clones fresh)
build/
# Per-slice build output
ci/output/
ci/build.log
# Release-only artifact (regenerated by CI); the .xcframework itself IS committed
OpenSSL.xcframework.zip
.DS_Store
.swiftpm/
