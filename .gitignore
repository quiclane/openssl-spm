# Local OpenSSL source checkout (build input; CI clones fresh)
build/
# Per-slice build output + logs
ci/output/
ci/*.log
# Built binaries — NOT committed (url-based binaryTarget; shipped as release assets)
OpenSSL.xcframework.zip
OpenSSL.xcframework/
libssl.a
libcrypto.a
include/
.DS_Store
.swiftpm/
